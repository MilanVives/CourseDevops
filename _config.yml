# Jekyll configuration for GitHub Pages
theme: jekyll-theme-primer

# Exclude files that contain Helm template syntax that conflicts with Liquid
exclude:
  - 07-Helm/
  - vendor
  - .bundle
  - Gemfile
  - Gemfile.lock
